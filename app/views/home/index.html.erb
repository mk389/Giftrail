<div class="bg-gray-200 py-10">
  <div class="flex items-center justify-center">
    <%= image_tag 'logo.svg', alt: 'ロゴ', class: 'px-0 py-0 mb-2' %>
  </div>

  <div class="flex items-center justify-center">
    <h2 class="text-3xl md:text-4xl font-bold text-center">「お土産選びをもっと楽しく、便利に」</h2>
  </div>

  <div class="flex items-center justify-center mt-2">
    <p class="font-mono text-lg md:text-xl text-gray-700 text-center">ユーザー同士でお土産や手土産の情報を簡単に共有できるアプリです。</p>
  </div>
  
  <div class="flex flex-col sm:flex-row justify-center mt-7 mb-20">
    <%= form_with url: posts_path, method: :get, local: true do |f| %>
      <div class="flex">
        <%= f.text_field :query, placeholder: "検索してみる 例:チョコレート", class: "border border-gray-300 rounded-l-md py-2 px-4 w-full sm:w-80 md:w-96 focus:outline-none focus:ring-2 focus:ring-blue-500" %>
        <button type="submit" class="bg-blue-500 text-white rounded-r-md py-2 px-4 hover:bg-blue-600">
          <i class="fas fa-search"></i>
        </button>
      </div>
    <% end %>
  </div>

  <div class="flex justify-center mb-10">
    <% if user_signed_in? %>
      <%= link_to "投稿してみる", new_post_path, class: "btn btn-accent text-sm sm:text-base py-2 px-4" %>
    <% else %>
      <%= link_to "投稿してみる", new_user_session_path, class: "btn btn-accent text-sm sm:text-base py-2 px-4" %>
    <% end %>
  </div>
</div>
